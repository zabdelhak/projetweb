document.getElementById("pick_up").value=map.location.latitude;
          document.getElementById("pick_btn").value=map.location.latitude; 
          if (document.getElementById("pick_up_btn").style.display == "none")
          {
            document.getElementById("drop").value=map.location.latitude;
            document.getElementById("drop_btn").value=map.location.latitude;
            const element=document.getElementById("drop_btn");
              element.addEventListener('onclick', myfonction(element));
          }
          else
          {
            document.getElementById("pick_up").value=map.location.latitude;
            document.getElementById("pick_up_btn").value=map.location.latitude; 
            const element=document.getElementById("pick_up_btn");
              element.addEventListener('onclick', myfonction(element));
          }
